import 'package:flutter/material.dart';

class GeneralSettingsCard extends StatelessWidget {
  const GeneralSettingsCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('一般設定'));
  }
}
