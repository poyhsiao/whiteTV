import 'package:flutter/material.dart';

class PlaybackSettingsCard extends StatelessWidget {
  const PlaybackSettingsCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('播放設定'));
  }
}
