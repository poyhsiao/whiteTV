import 'package:flutter/material.dart';

class DisplaySettingsCard extends StatelessWidget {
  const DisplaySettingsCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('顯示設定'));
  }
}
